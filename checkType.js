// Instruction
// Create a function that check if an input is a number

function checkNumber() {
  // CODE HERE
  //   if (typeof input === "number") {
  //     console.log(true);
  //   } else {
  //     console.log(false);
  //   }

  // ternary operator
  // condition ? true (saat sesuai) : false (saat tidak sesuai)

  // number > 0 , true
  // string length > 0, true
  let a = "a";

  a == true ? console.log(true) : console.log(false);
}

checkNumber();

// example
// input = "A" should return FALSE
// input = 10 should return TRUE
